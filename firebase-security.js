// Public client-side security configuration. The App Check site key is not a secret.
// Create a reCAPTCHA Enterprise site key, register it in Firebase App Check,
// then paste it here before enabling App Check enforcement.
window.TRAM_FIREBASE_SECURITY = Object.freeze({
    appCheckSiteKey: "",
});